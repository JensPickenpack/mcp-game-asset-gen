{
  "name": "mcp-game-asset-gen",
  "version": "0.1.0",
  "description": "MCP server for asset generation - image, video, audio, and 3D APIs for game development",
  "main": "dist/index.js",
  "type": "module",
  "repository": {
    "url": "https://github.com/Flux159/mcp-game-asset-gen"
  },
  "bin": {
    "mcp-asset-gen": "dist/index.js"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "AGENTS.md",
    "schemas/**/*"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc",
    "dev": "npm run build && npm run start",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:integration": "NO_MOCK_PROVIDERS=true vitest run tests/integration.test.ts",
    "test:integration:refresh": "NO_MOCK_PROVIDERS=true FORCE_REAL_API_TESTS=true vitest run tests/integration.test.ts",
    "test:integration:cleanup": "NO_MOCK_PROVIDERS=true CLEANUP_TEST_FILES=true vitest run tests/integration.test.ts",
    "test:all": "vitest run",
    "lint": "eslint src tests --ext .ts",
    "typecheck": "tsc --noEmit",
    "version:update": "node scripts/update-version.js"
  },
  "keywords": [
    "mcp",
    "asset-generation",
    "image",
    "video",
    "audio",
    "3d",
    "game-development"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "canvas": "^3.2.0",
    "dotenv": "^16.4.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}